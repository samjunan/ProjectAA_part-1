//  create an array of colors (4 colors)
//  use array methods to add a color to end of that array
//  user array method to add to the begining of that array
//  check the length of that array

let colors = ['red','green','blue','yellow'];

colors.push('white');

colors.unshift('purple');

console.log(colors);

console.log(`length is ` + colors.length);